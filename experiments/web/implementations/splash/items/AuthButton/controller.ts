import { Item_AuthButton } from "../../generated";

export const controller = Item_AuthButton.$controller({
  select({ props, state, context }) {
    return { props, state, context };
  },
  actions() {
    return {};
  },
  lifecycle: {},
});
