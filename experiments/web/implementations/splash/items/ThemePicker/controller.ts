import { Item_ThemePicker } from "../../generated";

export const controller = Item_ThemePicker.$controller({
  select({ props, state, context }) {
    return { props, state, context };
  },
  actions() {
    return {};
  },
  lifecycle: {},
});
