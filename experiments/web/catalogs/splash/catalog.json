{
  "version": 1,
  "id": "splash",
  "name": "Splash",
  "description": "Experimental catalog for splash and hero-style landing surfaces.",
  "props": {
    "locale": {
      "type": "string",
      "description": "Current locale for copy and formatting.",
      "default": "en-US"
    }
  },
  "state": {
    "loggedIn": {
      "type": "boolean",
      "description": "Whether the current scene viewer is logged in.",
      "default": false
    },
    "username": {
      "type": "string",
      "description": "Display name for the logged-in viewer."
    },
    "themeName": {
      "type": "string",
      "description": "Current theme selected for the scene.",
      "enum": ["light", "dark"],
      "default": "light"
    }
  },
  "actions": {
    "login": {
      "title": "Login",
      "description": "Updates scene-level authentication state for the current viewer.",
      "props": {
        "username": {
          "type": "string",
          "required": true,
          "description": "Display username."
        }
      }
    },
    "logout": {
      "title": "Logout",
      "description": "Clears scene-level authentication state for the current viewer.",
      "props": {}
    },
    "openUrl": {
      "title": "Open URL",
      "description": "Requests navigation to a URL selected from a splash call to action.",
      "props": {
        "url": {
          "type": "string",
          "required": true,
          "description": "Destination URL."
        },
        "target": {
          "type": "string",
          "description": "Navigation target hint.",
          "enum": ["self", "blank"],
          "default": "self"
        }
      }
    },
    "setTheme": {
      "title": "Set Theme",
      "description": "Updates the active scene theme.",
      "props": {
        "theme": {
          "type": "string",
          "required": true,
          "enum": ["light", "dark"],
          "description": "Theme name to activate."
        }
      }
    }
  },
  "themes": {
    "default": "light",
    "available": ["light", "dark"]
  },
  "items": ["Hero", "CallToAction", "Background", "Stack", "Counter", "Modal", "BetaSignupForm", "AuthButton", "ThemePicker"]
}
